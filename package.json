{
  "name": "hianime-v2",
  "version": "2.2.0",
  "type": "module",
  "scripts": {
    "dev": "bun run --hot index.ts",
    "start": "node dist/index.js",
    "build": "tsc && ncc build index.ts -o dist",
    "vercel-build": "npm run build",
    "lint": "eslint . --fix",
    "format": "prettier --write .",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "hono": "^4.6.0",
    "axios": "^1.7.9",
    "cheerio": "^1.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@vercel/ncc": "^0.38.4",
    "typescript": "^5.3.3",
    "eslint": "^8.57.0",
    "prettier": "^3.2.5"
  }
}
